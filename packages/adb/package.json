{
  "name": "@android-devkit/adb",
  "version": "0.1.0",
  "description": "ADB (Android Debug Bridge) wrapper library",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@android-devkit/android-sdk": "workspace:*",
    "@yume-chan/adb": "^2.5.1",
    "@yume-chan/adb-server-node-tcp": "^2.5.2",
    "@yume-chan/stream-extra": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0"
  },
  "files": ["dist"]
}
